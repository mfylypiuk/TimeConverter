<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TimeConverter.Views.ConverterPage"
             Title="Converter">
    
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Back" Clicked="Back_Clicked" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Entry x:Name="NumberOfTimesEntry" FontSize="Large" Grid.Column="0" TextChanged="NumberOfTimesEntry_TextChanged" />
            <Label x:Name="FromType" Text="TimeType" FontSize="Large" HorizontalOptions="Center" Grid.Column="1" />
            <Label Text="=" FontSize="Medium" HorizontalOptions="Center" Grid.Column="1" />
            <Label x:Name="Result" Text="x TimeType" FontSize="Large" HorizontalOptions="Center" Grid.Column="1" />
        </StackLayout>
    </ContentPage.Content>
    
</ContentPage>
